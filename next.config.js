/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "cdn.sanity.io",
            "themeger.shop"
        ],
    }
}

module.exports = nextConfig
